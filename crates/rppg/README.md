# crates/rppg

rPPG DSP core for Elata SDK.

This crate implements the rPPG pipeline: buffering, temporal normalization, bandpass filtering, and HR estimation. Tests follow TDD; start by running `cargo test` in the crate root.